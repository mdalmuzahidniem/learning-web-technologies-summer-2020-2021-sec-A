<?php
	/*session_start();

	if(isset($_POST['delete'])){
			
			$username= $_REQUEST['name'];
			$password= $_REQUEST['password'];
			$type= $_REQUEST['type'];
			
			$user=null;
			$file=fopen('appointment.txt','w');
			fwrite($file, $user);
			fclose($file);
			
			header('location: ../view/userHistory.php?name='.$username.'&password='.$password.'&type='.$type.'&msg=done');
			
	}else{
		echo "invalid request...";
	}
	*/

?>